# v4.4.1 Team Rules and Domination Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Repair map-safe energy spawning, team-aware bot combat, visible team identity, and map-correct domination rules without reducing rendering quality.

**Architecture:** Keep authoritative rules in shared map/capture configuration and the server simulation. Expose only immutable snapshot data to clients; use small client helpers for consistent team labels. Preserve existing collision and interpolation systems.

**Tech Stack:** TypeScript, Vitest, Phaser, Vite, Playwright/browser screenshots.

---

### Task 1: Map-safe objective configuration

**Files:** `src/shared/map-catalog.ts`, `src/shared/capture-point.ts`, `src/server/simulation.ts`, `src/server/bot.ts`, `tests/simulation.test.ts`, `tests/capture-point.test.ts`

- [ ] Add `capturePointCenter` to every map definition and use safe neon/crystal centers.
- [ ] Store the map-specific capture config on `GameWorld`; pass it to capture advancement and snapshots.
- [ ] Filter energy spawn candidates with `circleHitsRect(point, ENERGY_RADIUS, wall)`.
- [ ] Make bot objective movement use the world capture center.
- [ ] Add tests for safe energy points, safe centers, capture/contest/neutralize/scoring behavior.

### Task 2: Team-aware bot targeting

**Files:** `src/server/bot.ts`, `src/shared/mode-catalog.ts`, `tests/bot.test.ts`

- [ ] Add a pure `selectCombatTarget` helper that excludes same-team alive players in team modes.
- [ ] Keep solo behavior unchanged and test the case where an ally is closer than the enemy.

### Task 3: Team identity in lobby and match HUD

**Files:** `src/client/mobile-app.ts`, `src/client/game-scene.ts`, `src/client/render-throttle.ts`, `src/client/styles.css`, `tests/tactical-hud-ui.test.ts`, `tests/render-throttle.test.ts`

- [ ] Add a shared client `teamLabel` helper and show it in lobby roster, leaderboard, team score, and player nameplates.
- [ ] Include team IDs in render revisions so team assignment changes repaint immediately.
- [ ] Keep labels legible on desktop and landscape mobile without covering controls.

### Task 4: Verification, visual QA, and release

- [ ] Run focused tests, typecheck, full tests, production build, load/diagnostic tests, and clean-clone smoke test.
- [ ] Run browser screenshots at 667×375, 844×390, and 932×430 for team roster, team HUD, and domination map.
- [ ] Update version to `4.4.1`, README, and CHANGELOG.
- [ ] Commit and push the verified release to `origin/main`.
